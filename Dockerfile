# Установка базового образа (Node.js LTS)
FROM node:lts

# Создание директории приложения в образе
WORKDIR /app

# Установка зависимостей приложения
COPY package*.json ./
RUN npm install

# Копирование исходного кода приложения в образ
COPY . .

# Указание порта, который будет прослушиваться внутри контейнера
EXPOSE 3000

# Команда для запуска приложения в контейнере
CMD ["npm", "start"]